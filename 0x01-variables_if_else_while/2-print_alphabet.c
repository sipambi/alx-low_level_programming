#include <stdlib.h>
#include <stdio.h>

/**
 * main - checking for the last digit
 * Return: 0 if exited properly
 */
int main(void)
{
	char letter;
	for(letter = 'a'; letter <= 'z'; letter++)
		putchar(letter);
	return (0);
}
