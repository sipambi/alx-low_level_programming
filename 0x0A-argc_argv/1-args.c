#include "main.h"
/**
 * main - prints the number of arguments 
 * @argc: first arg
 * @argv: second arg
 * Return 0: if no errors
 */

int main(int argc, char *argv[])
{
	printf("%d\n" , argc);
	return (0);
}
